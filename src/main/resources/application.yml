server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/clinic?useSSL=false
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

#java -jar -Dspring.profiles.active=local rest-api-server-0.0.1-SNAPSHOT.jar
